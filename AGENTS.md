# Agents — Team Roster

Canonical reference for every agent in this team. For each agent: role, model, where to find them, when they fire.

---

## The Founding Team (8 agents)

### CEO-facing voices (you talk to these)

| Agent | Role | File | Model | Slash command | Fires |
|---|---|---|---|---|---|
| **Intake** | Director of Product Discovery | [`agents/intake.md`](agents/intake.md) | Opus | `/intake`, `/team` | New work, scope shifts, resume from pause |
| **Executive Assistant** | Chief of Staff | [`agents/executive-assistant.md`](agents/executive-assistant.md) | Sonnet | `/status`, `/briefing`, `/queue`, `/inbox` | Session start/close, hard checkpoints, blockers, on-demand |

### Backstage agents (work behind the scenes)

| Agent | Role | File | Model | Slash command | Fires |
|---|---|---|---|---|---|
| **Conductor** | CTO/CPO | [`agents/conductor.md`](agents/conductor.md) | Sonnet | (internal) | Every agent completion, state update, approval |
| **Strategist** | VP Product | [`agents/strategist.md`](agents/strategist.md) | Opus | `/strategist` | Phase = briefed/stratego |
| **Architect** | VP Engineering | [`agents/architect.md`](agents/architect.md) | Opus | `/architect` | Phase = prd-ok/scoping/planned/scaffold |
| **Designer** | UX + design system | [`agents/designer.md`](agents/designer.md) | Opus | `/designer` | Parallel to Strategist/Architect when project has UI scope (activated 2026-05-05) |
| **Critic** | Independent advisor | [`agents/critic.md`](agents/critic.md) | Opus | `/critic` | Any artifact written/updated, parallel to producers |
| **Org Designer** | Head of People | [`agents/org-designer.md`](agents/org-designer.md) | Opus | `/org-designer`, `/grow-team` | Project retro, /grow-team, accumulated triggers |

---

## Model Strategy

**Opus** — agents that reason hard, judge taste, find subtle issues, write substantive content
- Intake, Strategist, Architect, Critic, Org Designer

**Sonnet** — agents that route, summarize, structure, do mostly deterministic work
- Conductor, Executive Assistant

This split keeps cost down on routing/summarization while preserving quality on the agents that actually shape product decisions. Override per-agent in frontmatter if you want to tune.

---

## Communication Map

```
You (CEO)
   │
   ├──→ INTAKE ──→ briefs Strategist via Conductor
   │
   ├──← EA ←── reads everything, surfaces decisions
   │
   ▼
CONDUCTOR (router) ──→ STRATEGIST ──→ ARCHITECT ──→ Tier 2 scaffold
                            │              │
                            ▼              ▼
                          CRITIC (parallel review of both)
                            │
                            ▼
                       critic-notes.md
                            │
                            ▼
                       Conductor consistency check
                            │
                            ▼
                       EA surfaces hard checkpoints
                            │
                            ▼
                          You decide
                            │
                            ▼
                       Conductor advances state

(Slow cadence — separate loop)
   ORG DESIGNER ──→ proposals ──→ EA surfaces team-health
```

**Two CEO-facing voices: Intake (down) and EA (up). Five backstage. Org Designer is the meta-layer.**

---

## Planned (not yet activated — 9 stubs)

These live as stubs in `agents/_planned/`. See [`agents/_planned/README.md`](agents/_planned/README.md) for activation triggers.

### Post-shipping
| Stub | Activates when |
|---|---|
| **GTM Strategist** | First project ships |
| **Growth Analyst** | First project has measurable users |
| **Feedback Synthesizer** | First project receives user feedback |

### Depth-on-demand specialists
| Stub | Activates when |
|---|---|
| **Customer Researcher** | Strategist's customer research repeatedly flagged shallow |
| **Industry Researcher** | Strategist's competitive scans repeatedly flagged shallow |
| **Designer** | UX/design decisions become load-bearing |
| **Ops/Security** | First project handling sensitive data, or repeated security gaps |
| **Biz/Finance** | First non-trivial pricing model |
| **Biz/Legal** | First regulated-domain project or content-licensing requirement |

Org Designer proposes activation when triggers fire. User approves → stub moves from `_planned/` to `agents/` and gets fleshed out.

---

## Tier 2 Agents (per-project)

Each project's own `.claude/` (generated by Architect at scaffold phase) has its own agents, tuned to the project's stack. They live in `<project-repo>/.claude/agents/`. Their templates are in [`templates/stacks/`](templates/stacks/) — populated as Architect encounters new stacks.

**Tier 1 (this directory) ≠ Tier 2 (per project).** This file documents Tier 1 only.

---

## Private-data discipline for framework authors (MANDATORY)

Any agent (or operator) that writes a framework file which propagates to the public mirror MUST author it clean: **never type real private identifiers** — infra hostnames / DB-endpoint IDs, prod deploy URLs, internal project codenames/slugs, `<org>/<repo>` paths for private repos, operator home paths/usernames. Use placeholders (`<project>`, `<org>/<project>`, `<neon-endpoint>`, `<deploy-url>`, `<operator>`, `<framework-root>`).

The publish pipeline genericizes a known identifier set (`scripts/sync-src/manifest.json5 → genericize`) and three guards back it — the `bare-codename` sync lint, `npm run verify-genericize`, and the `framework-private-data-gate.py` pre-write hook — but the genericizer only knows the identifiers already in its map. **Author clean; never rely on the sweep.** A brand-new identifier class slips through every automated layer until it's added to the map.

Provenance: 2026-06-02 sync-genericizer remediation. See the framework `CLAUDE.md` "Private-data discipline" section for the full rule.

---

## Updating This File

When the team changes (agent split, new role activated, prompt rewrite), Org Designer updates this file as part of its proposal flow. User approval required. Logged in `memory/agent-changelog.md`.

Keep this file accurate. It's the canonical roster — onboarding docs, slash command help, and quick references all derive from it.
